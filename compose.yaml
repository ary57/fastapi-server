services:
  webapp:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DEBUG: "true"
    volumes:
      - .:/app
    command: uv run uvicorn main:app --host 0.0.0.0 --port 8000